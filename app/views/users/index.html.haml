%h1 Пользователи


.list-group
  - @users.each do |user|
    = link_to(user, class: 'list-group-item') do
      = user.last_name
      = user.first_name
      = user.second_name
      .clearfix


/= link_to 'New User', new_user_path
