= form_for @message_list do |f|
  - if @message_list.errors.any?
    #error_explanation
      %h2= "#{pluralize(@message_list.errors.count, "error")} prohibited this message_list from being saved:"
      %ul
        - @message_list.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :header
    = f.text_field :header
  .actions
    = f.submit 'Save'
